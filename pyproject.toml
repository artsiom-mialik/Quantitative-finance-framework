[project]
name = "quantitative-finance-framework"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "numpy",
    "scipy",
    "matplotlib",
    "pytest",
]

[tool.pytest.ini_options]
pythonpath = ["src"]